name: workflowevents

on: 
 workflow_dispatch: 

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@04
      - name: verify index.html
        run: |
          echo "checking index.html.."
          test -f index.html && echo "file exits"
      
